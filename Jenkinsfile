pipeline{

    agent any

    stages{
        stage("build"){
            steps{
                echo "========executing build========"
            }
        }

        stage("deploy"){
            steps{
                echo "========executing deploy========"
            }
        }

        stage("prune"){
            steps{
                echo "========executing prune========"
            }
        }
    }
}